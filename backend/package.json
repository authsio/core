{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "tsc",
    "build": "rimraf ./dist && npm run tsc",
    "start:dev": "nodemon"
  },
  "keywords": [],
  "author": "@hkd987",
  "devDependencies": {
    "@types/node": "^17.0.10",
    "esbuild": "^0.14.12",
    "esbuild-node-tsc": "^1.8.2",
    "nodemon": "^2.0.15",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "fastify": "^3.27.0"
  }
}
